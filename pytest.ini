[pytest]
# Pytest configuration for aerpawlib tests

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
log_cli = true
log_cli_level = DEBUG

# Async mode
asyncio_mode = auto

# Markers
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires SITL)

# Default options
addopts =
    -v
    --tb=short
    -ra
    -o log_cli=true
    -o log_cli_level=INFO
